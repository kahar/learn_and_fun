clean, build and run whole project ( gradlew clean build run  ) - tested only on winows :)

There is jarProducer - which produce jar, main project open this jar (using URL) and add class from this project to
classpath.